---
title: "segunda Sesion"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Ctrl+Shift+Enter*. 

```{r}
x = TRUE; y = FALSE;
x1 = T ; y1= F
x;y;x1;y1
```
```{r}
x2 = c(T,F,T,F);
y2 = c(T,T,F,F)
x2 ; y2
```
```{r}
2*x2 ; -3*y2;
```
```{r}
3==4;
4<=5;
3!=5
```
```{r}
x2 = c(T,F,T,F);
y2 = c(T,T,F,F)
x2 & y2
```

```{r}
x2 = c(T,F,T,F);
y2 = c(T,T,F,F)
x2 | y2
```
```{r}
x2 = c(T,F,T,F);
y2 = c(T,T,F,F)
!(x2 | y2)
```

```{r}
x = seq(-5 , 5 , 0.1)
y = x^2+1
plot(x,y,col="red" , type= "l" , lwd=2, las = 1 , cex.axis=1.5 , xlab = "ejex ", ylab = "eje y",
     cex.lab = 1.5)
```
el comando general para seleccionar una muestra de un conjunto de datos en 'R' es sample.
Si el muestreo es con reemplazo entonces se usa el arguemento 'replace = true' si es sin reemplazo se indica replace = FALS   

```{r}
z= 1:20
#con reemplazo
sample(z,7,replace = T)
#sin reemplazo
sample(z,7,replace = F)
```

```{r}
#evento A
#evento A salen 3 caras = 1/8
sample(c("C","S"),3,replace=T)
```
```{r}
#cara = 1 sello = 0
n=1000
xs = numeric(n)
for(i in 1:n){
  trial = sample(c(1,0),3,replace=TRUE)
  if(sum(trial)==3) 
    xs[i]=1 
  else 
    xs[i]=0
}
mean(xs)
plot(1:n,cumsum(xs)/1:n, col="red",type="l",lwd=1.3)
abline(h=0.125 , col="blue" ,lwd=1.5)

```
```{r}

```























